{
  "name": "lzt-ui",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter lit-components dev",
    "dev:react": "pnpm --filter react-demo dev",
    "build": "pnpm --filter lit-components build",
    "storybook": "pnpm --filter lit-components storybook",
    "build-storybook": "pnpm --filter lit-components build-storybook"
  }
}
